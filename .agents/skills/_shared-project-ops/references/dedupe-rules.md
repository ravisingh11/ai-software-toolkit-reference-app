# Dedupe Rules

- Prefer one canonical finding per root cause.
- Merge overlapping bug and security findings when the same defect creates both outcomes.
- Keep the most actionable title and ID; append additional evidence under that entry.
- Do not create a new GitHub issue when an existing open issue already tracks the same root cause.
- If two reports share symptoms but not root cause, keep them separate.
